CREATE TABLE USER
(
    ID INTEGER AUTO_INCREMENT PRIMARY KEY NOT NULL,
    EMAIL VARCHAR(255) NOT NULL,
    PASSWORD_HASH VARCHAR(255) NOT NULL,
    ROLE VARCHAR(10) NOT NULL
);
CREATE UNIQUE INDEX UNIQUE_EMAIL_INDEX_2 ON USER (EMAIL);


